apiVersion: v1
kind: Service
metadata:
  name: keyservice
  namespace: deployments
spec:
  type: ClusterIP
  selector:
    app: keyservice
  ports:
    - name: http
      port: 80
      targetPort: 50052
