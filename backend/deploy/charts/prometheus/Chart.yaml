apiVersion: v2
name: prometheus-server
description: A Helm chart for deploying Prometheus configured to scrape annotated services (like your Fastify app)
type: application
version: 0.1.0
appVersion: "2.41.0"
