apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: fast
provisioner: kubernetes.io/gce-pd # Use GCE persistent disks for example
parameters:
  type: pd-ssd # Optional, specifies the disk type
