apiVersion: v1
kind: Service
metadata:
  name: authentservice
  namespace: deployments
spec:
  type: ClusterIP
  selector:
    app: authentservice
  ports:
    - name: http
      port: 80
      targetPort: 50051 # Ensure this matches the container's exposed port
