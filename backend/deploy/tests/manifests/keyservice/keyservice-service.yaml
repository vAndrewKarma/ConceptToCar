apiVersion: v1
kind: Service
metadata:
  name: keyservice
  namespace: deployments
spec:
  type: ClusterIP
  selector:
    app: keyservice
  ports:
    - name: http
      port: 81
      targetPort: 50051 # FIXED: Use correct container port
